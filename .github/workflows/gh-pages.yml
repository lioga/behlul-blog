name: Deploy to GitHub Pages

on:
  push:
    branches: main
  workflow_dispatch:

jobs:
  deploy:
    uses: lioga/behlul-blog/.github/workflows/gh-pages.yml@main
    permissions:
      contents: write
    with:
      publish_dir: build
